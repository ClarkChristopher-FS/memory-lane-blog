// Next.js config for static export
// This makes the site export as static HTML files

const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
